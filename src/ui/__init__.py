"""
MeetingRecorder UI layer — customtkinter widgets and theme.

All code in this package runs on T1 (the Tk mainloop thread).
Worker threads must never import or call UI classes directly;
they must dispatch via AppWindow.dispatch(fn).
"""
